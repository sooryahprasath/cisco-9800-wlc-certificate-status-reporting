# Cisco 9800 WLC Certificate Status Reporter Configuration
# ============================================================
# IMPORTANT: Copy this file to .env and update with your actual values
# Never commit the .env file to version control!

# Email Configuration (REQUIRED for email reports)
# =================================================
SENDER_EMAIL=your-email@example.com
SENDER_PASSWORD=your-app-password-here
DEFAULT_RECIPIENTS=recipient1@example.com,recipient2@example.com

# Gmail App Password Instructions:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and "Other (Custom name)"
# 4. Generate password and copy it here (16 characters, no spaces)

# SMTP Configuration (Optional - defaults shown)
# ==============================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# For other email providers:
# Office 365: smtp-mail.outlook.com, port 587
# Yahoo: smtp.mail.yahoo.com, port 587
# Custom SMTP: your-smtp-server.com, port 587

# File Configuration
# ==================
WLC_DATA_FILE=wlc.json
LOG_DIR=logs

# Processing Configuration
# ========================
MAX_THREADS=10
COMMAND_TIMEOUT=180
CONNECTION_TIMEOUT=180
PING_TIMEOUT=5
MAX_RETRIES=3

# Report Configuration
# ====================
# Timezone for report timestamps (examples: UTC, America/New_York, Europe/London, Asia/Singapore)
REPORT_TIMEZONE=UTC

# Advanced Configuration (Optional)
# ==================================
# Uncomment and modify these if needed:

# WLC Connection Settings
# GLOBAL_DELAY_FACTOR=3
# MAX_LOOPS=2000
# DELAY_FACTOR=20
# KEEPALIVE=30
# AUTH_TIMEOUT=30
# BANNER_TIMEOUT=30

# Logging Configuration
# LOG_LEVEL=INFO
# ENABLE_SESSION_LOGS=true

# Performance Tuning
# THREAD_POOL_SIZE=10
# CONNECTION_POOL_SIZE=50

# Security Notes:
# ==============
# 1. Set secure file permissions: chmod 600 .env
# 2. Add .env to your .gitignore file (already included)
# 3. Never share or email this file
# 4. Use app passwords for email authentication (not your account password)
# 5. Rotate passwords regularly (every 90 days recommended)
# 6. Consider using a secrets management system for production
# 7. The script encrypts WLC passwords in memory using AES-128-CBC
# 8. PBKDF2 key derivation uses 100,000 iterations (OWASP recommended)
# 9. Session timeout prevents long-term credential exposure
# 10. Core dumps are disabled to prevent password leakage

# Production Security Recommendations:
# ===================================
# 1. Use dedicated service accounts for WLC access
# 2. Implement network segmentation (VLANs, firewalls)
# 3. Enable logging and monitoring on WLC devices
# 4. Use VPN or jump servers for WLC access
# 5. Implement role-based access control (RBAC)
# 6. Regular security audits and credential rotation
# 7. Monitor for unusual access patterns
# 8. Use encrypted communication channels
# 9. Implement backup and disaster recovery procedures
# 10. Document security procedures and incident response